
import React from 'react';

const MapControlPanel: React.FC = () => {
  return null; // Remove the entire control panel since radius controls were the only content
};

export default MapControlPanel;
